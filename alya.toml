[package]
name = "term"
version = "0.1.0"
alya-version = "0.0.16"
entry = "src/lib.alya"
description = "Modern terminal UI toolkit: ANSI styling, Unicode tables, callout boxes, progress bars, and prompts"
authors = ["Alya Language Contributors <https://github.com/alya-lang>"]
license = "MIT"
homepage = "https://github.com/alya-lang/alya"
repository = "https://github.com/alya-lang/term"
keywords = ["alya", "alya-lang", "package", "term", "terminal", "table", "ansi", "tui", "prompt", "cli"]

[dependencies]

# [build]
# c-sources = ["c/source.c"]
# c-flags = ["-O2"]
# c-include-dirs = ["c"]
